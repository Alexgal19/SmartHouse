

export default function DashboardPage() {
    // This is a server component and is only used to avoid a 404 when the non-localized route is accessed.
    // The actual content is in /[locale]/dashboard/page.tsx
    // In a real application, you might want to add a redirect here.
    return null;
}
